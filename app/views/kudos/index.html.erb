<p id="notice"><%= notice %></p>

<h1>Listing Kudos</h1>

<table>
  <thead>
    <tr>
      <th>Description</th>
      <th>Organization</th>
      <th>Given by</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @kudos.each do |kudo| %>
      <tr>
        <td><%= kudo.description %></td>
        <td><%= kudo.organization_id %></td>
        <td><%= kudo.given_by_id %></td>
        <td><%= link_to 'Show', kudo %></td>
        <td><%= link_to 'Edit', edit_kudo_path(kudo) %></td>
        <td><%= link_to 'Destroy', kudo, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Kudo', new_kudo_path %>
